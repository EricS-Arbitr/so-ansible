---
# =========================================================================
# so_search — DEFERRED PENDING PEXPECT WRAPPER (as of 2026-07-22)
# See roles/so_manager/tasks/main.yml for context.
# =========================================================================

- name: STUB — so_search pending pexpect wrapper
  ansible.builtin.fail:
    msg: |
      so_search role is intentionally stubbed pending the pexpect wrapper
      rewrite scoped for the next session.
