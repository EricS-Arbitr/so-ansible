---
# =========================================================================
# so_sensor — DEFERRED PENDING PEXPECT WRAPPER (as of 2026-07-22)
#
# Note: this role's GRE tunnel setup is still valuable and independent of
# the SO install path. Preserved verbatim below the fail-stub — remove
# the fail block + un-comment the tunnel tasks if you want to bring up
# just tun0 for network testing without running so-setup.
# =========================================================================

- name: STUB — so_sensor pending pexpect wrapper
  ansible.builtin.fail:
    msg: |
      so_sensor role is intentionally stubbed pending the pexpect wrapper
      rewrite scoped for the next session. The GRE tunnel setup portion is
      still useful stand-alone; extract from git history at commit 33517a0
      if you need just tun0 up for testing.
