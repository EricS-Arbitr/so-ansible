---
# =========================================================================
# so_manager — DEFERRED PENDING PEXPECT WRAPPER (as of 2026-07-22)
# =========================================================================
# The current SO 2.4 branch (2.4/main) has no setup/automation/ dir. All
# previous work here (rendering answer files, invoking so-setup network
# <name>, polling for completion) assumed the master-branch answer-file
# mechanism, which turned out to be a 2.3.300 feature.
#
# Non-interactive SO 2.4 install requires a pexpect Python script to
# drive the whiptail TUI prompt-by-prompt. That work is scoped for a
# follow-up session.
#
# This stub fails loudly if invoked so a stale Phase 40 command doesn't
# silently do nothing.
# =========================================================================

- name: STUB — so_manager pending pexpect wrapper
  ansible.builtin.fail:
    msg: |
      so_manager role is intentionally stubbed pending the pexpect wrapper
      rewrite scoped for the next session. See UPSTREAM_FIXES.md · 2026-07-22
      + PROJECT_LOG.md for context. Do not run playbooks/40-manager.yml until
      the pexpect wrapper lands.
